# Barbershop Web

## Requirements

- Docker
- Docker-compose

## Code Quality

We are using `pylint` and `pytest` to enraise our code quality.
To run the lint use `pylint --load-plugins pylint_django apps/ configs/`.